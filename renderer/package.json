{
  "name": "mr-radar-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless NEXRAD radar image renderer for the mr-radar display",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "eslint .",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
